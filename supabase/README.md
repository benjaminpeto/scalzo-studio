# Supabase Workspace

This directory is reserved for local Supabase development assets.

## Folders

- `migrations/` for SQL migrations
- `seed/` for seed data and bootstrap scripts

Future schema, auth, RLS, and local database tasks should land here instead of being scattered through the app workspace.
