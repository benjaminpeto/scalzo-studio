{
  "name": "@scalzo/web",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --ignore-pattern .next --ignore-pattern next-env.d.ts",
    "lint:fix": "eslint . --fix --ignore-pattern .next --ignore-pattern next-env.d.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  }
}
